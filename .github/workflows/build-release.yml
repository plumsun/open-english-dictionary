name: Build Dictionary Archives

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      formats:
        description: 'Archive formats to build (space-separated)'
        required: false
        default: 'zip tar.gz tar.bz2 tar.xz'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Build archives
        run: |
          FORMATS="${{ github.event.inputs.formats || 'zip tar.gz tar.bz2 tar.xz' }}"
          uv run pack_dictionary.py --formats $FORMATS --output dist

      - name: Calculate checksums
        run: |
          cd dist
          sha256sum * > checksums.txt
          cat checksums.txt

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dictionary-archives
          path: dist/*
          retention-days: 30

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
          draft: false
          prerelease: false
